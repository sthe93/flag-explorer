{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for Flag Explorer App",
  "main": "src/app.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon src/app.js",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "prepare": "echo 'Skipping prepare hook during CI'",  
    "build": "tsc"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.31.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
